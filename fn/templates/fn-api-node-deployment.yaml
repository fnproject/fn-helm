apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}-api
  labels:
    app: {{ template "fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
  replicas: 1
  template:
    metadata:
       labels:
         app: {{ template "fullname" . }}
         chart: {{ .Chart.Name }}-{{ .Chart.Version }}
         heritage: {{ .Release.Service }}
         release: {{ .Release.Name }}
         role: fn-api
    spec:
      {{- if .Values.fn_api.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.fn_api.nodeSelector | indent 8 }}
{{- end }}
      {{- if .Values.fn_api.tolerations }}
      tolerations:
{{ toYaml .Values.fn_api.tolerations | indent 8 }}
{{- end }}
      containers:
        - name: fn-api
          image: {{ .Values.fn_api.image }}:{{ .Values.fn_api.tag }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          resources:
{{ toYaml .Values.fn_api.resources | indent 12 }}
          securityContext:
            privileged: true
          ports:
          - containerPort: {{ .Values.fn_api.container_port }}
          readinessProbe:
            httpGet:
              path: /v2/apps
              port: {{ .Values.fn_api.container_port }}
            initialDelaySeconds: 3
            periodSeconds: 3
          env:
          - name: FN_DB_PASSWD
            valueFrom:
              secretKeyRef:
                name: {{ .Release.Name }}-mysql
                key: mysql-password
          - name: FN_DB_HOST
            value: {{ .Release.Name }}-mysql
          - name: FN_MQ_HOST
            value: {{ .Release.Name }}-redis
          - name: FN_PORT
            value: {{ .Values.fn_api.container_port | quote }}
          - name: FN_NODE_TYPE
            value: "api"
          - name: FN_PUBLIC_LB_URL
            value: http://{{ .Values.fn_lb_runner.service.public_dns_name }}:{{ .Values.fn_lb_runner.service.port }}
          {{- range $key, $value := .Values.fn_api.env }}
            {{- if $value }}
          - name: {{ $key }}
            value: {{ $value | quote }}
            {{- end }}
          {{- end }}

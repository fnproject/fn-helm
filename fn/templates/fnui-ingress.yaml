{{- if .Values.fnui.ingress.enabled -}}
{{- $serviceName := printf "%s-ui" (include "fullname" .) -}}
{{- $servicePort := .Values.fnui.service.externalPort -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "fullname" . }}-ui
  labels:
    app: {{ template "fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  annotations:
    {{- range $key, $value := .Values.fnui.ingress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
{{- if .Values.fnui.ingress.hosts }}
  rules:
    {{- range $host := .Values.fnui.ingress.hosts }}
    - host: {{ $host.name }}
      http:
        paths:
          - path: {{ default "/" $host.path }}
            backend:
              serviceName: {{ $serviceName }}
              servicePort: {{ $servicePort }}
    {{- end -}}
{{- else }}
  backend:
    serviceName: {{ $serviceName }}
    servicePort: {{ $servicePort }}
{{- end }}
  {{- if .Values.fnui.ingress.tls }}
  tls:
{{ toYaml .Values.fnui.ingress.tls | indent 4 }}
  {{- end -}}
{{- end -}}
